<Window x:Class="furnitare.test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:furnitare"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="test">

    <Grid>
        <StackPanel Orientation="Horizontal" Margin="0,357,0,0">
            <TextBox x:Name="txtFilter" TextWrapping="Wrap" Width="119" Margin="0,10" TextChanged="txtFilter_TextChanged"/>

        </StackPanel>
        <ListView x:Name="Grofa" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="357">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Id_Furniture}" 
                      Header="IdFurniture" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Id_Postavshik}" 
                      Header="IdPostavshik" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Название}" 
                      Header="Название" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
